requests==2.31.0
python-dotenv==1.0.1
# QR codes for the welcome letter. Pure Python and writes the PNG itself,
# so no Pillow and nothing to compile on the server.
segno==1.6.6

# Веб-админка
fastapi==0.115.6
uvicorn[standard]==0.34.0
jinja2==3.1.5
python-multipart==0.0.20
itsdangerous==2.2.0
